# PR 2 — rounds 24-26 relational monitor（2026-09-08）

`policy:loop-monitor`。read-only、fresh context。観測範囲 **24-26**（26 側は
「round 25 の修正」。round 26 はまだ走っていない）。head `65dfdbe`。

主文脈は**前回監視の反証条件が発火した事実を明示して渡した** ——
「受理集合がまた変わったら DRIFTING」に対し、UTF-8 の修正は消費者由来の
受理集合変更である。見逃しを期待するのではなく、条件が意図した区別を
まだ切れているかを判定させた。

## VERDICT: CONVERGING / recommendation: **continue（D11 が選んだ範囲限定 round 26 を回す）**

## ラウンドごと

- **R24**（`5a96daa`、非限定）: blocking 2、どちらも `deliverable-path`、再現済み。
  修正 `68d4972` は production `+43/−6`、authorship 再帰 **0/2**。
- **R25**（`0c3eff8`、書き直した契約に対する最初のラウンド）: blocking 3、
  **3 件とも前回監視が CONVERGING と事前登録した形**。
  「§1/§3 の外側の値が検査を破る」形は **0 件**。authorship 再帰 **1/3**（指摘 2 ← `68d4972`）。
- **R25 の修正**（`65dfdbe`）: 4 ファイル。`param_domain.py`（+55/−30）、そのテスト、
  HISTORY +40、BLUEPRINT +3。**新モジュール・新 instrument・新ゲートはゼロ。**

## 収束の判定

**成果物は動き、周辺は動いていない。** blocking 数は横ばい（3, 2, 3）、authorship 再帰も
横ばい（1/3, 0/2, 1/3）でどちらも増えていない。`65dfdbe` は**クラス単位の修復**であって
個別パッチではない —— 述語は境界を言い直すのをやめて**正規化関数を呼ぶ**（宣言が 2 つ
あったのを 1 つに）、numpy 型軸はベタ書きをやめて**モジュールを読む**。
どちらも指摘 2 / 3 を生んだクラスそのものを消している。この窓の apparatus 増加はゼロ。

## 反証条件そのものの欠陥（監視が主文脈に返した指摘）

**3 つの監視が同じ条件を 3 通りに書いている**:
2325「2 つの `lgb.train` の外側の消費者」/ 2425「どの消費者であれ」/
今回「**ラウンド開始時点の §2 の表に無い消費者**」。
**毎ラウンド書き直される反証条件は停止規則ではない** —— それは
loop-monitor が探している escape の形そのものである。
**1 つの文言を固定して、次のラウンドの監視が 3 つではなく 1 つを継承するようにすること。**

証拠の側: 監視は `_c_str` を独立に確認した（`lightgbm/basic.py:515` が UTF-8 encode、
`:1967→1977` と `:2163→2177` で `_param_dict_to_str` の直後に置かれる）。
**指摘 1 の 2 つの sink は、round 25 開始時点で既に §2 に載っていた消費者に属する**
（`export_code` は `8743c7c` で採用済み）。`65dfdbe` の HISTORY 差分もそれを裏づける ——
表に増えたのは**既存の消費者に対する要件の行**で、**消費者の行は 0**。
2325 の字義どおりの読みは自滅的でもある: `export_code` を採用した以上、
その後のどんな精緻化も永遠に (b) を発火させ、採用が無意味になる。
(b) の目的は「**提案が名指していない第 3 の消費者が受理集合を決めている**」ことであり、
それは起きていない。

**停止規則の無い唯一の軸**は、H-0095 §2 自身が消費者リストを開いていると宣言している
こと（「閉じられるのは要件のリストであって消費者のリストではない」）。
どの文言を採っても、drift が入るのはここだけである。

## round 26 のための識別子と停止トリガ

- **ラウンド開始時点の §2 の表に無い sink** を名指す指摘 → 受理集合を「文句を言った者」が
  書いている → **DRIFTING**。
- **表に載っている**消費者の要件が実測で不十分、または §2/§3 が標本の上で不完全
  → 契約の内側 → `REQUEST_CHANGES` でも CONVERGING。
- **take-stop トリガ**: round 26 が **`65dfdbe` が書いたコードの欠陥**を出したら、
  それは**クラス単位の修復のあとの 2 連続 authorship 再帰**であり、修正品質の信号である。
  round 27 を開かず止めること。

## 主文脈の処置

**continue を採用**し、**反証条件の文言を D11 に固定した**（下記）。
round 26 は D11 の決定どおり**範囲限定**で回す。
