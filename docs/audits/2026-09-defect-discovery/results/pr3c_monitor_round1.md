# PR 3c 設計レビュー — ループ監視（round 1 のあと、2026-09-15）

`policy:loop-monitor` に従い、round 2 の前に absolute monitor を回した。
read-only、fresh context（Codex `gpt-6-astra` effort low）。
capsule は `scratchpad/codex-monitor-prompt-pr3c-round1.md`（この session の scratchpad）。

主コンテキストは 1 つの事実を自己申告した: round 1 のあと、round 1 の指摘ではない理由で設計が広がった
（Platt intercept の調査 → 管理者が本 PR で既定値を原典に寄せ、自前の Platt MLE に置き換えると決定 →
artifact の移行が必要になった）。

## VERDICT: **DELIVERABLE-FOCUSED** ／ Recommendation: **`redirect`**

## 分類（監視の判定）

| 対象 | 判定 |
|---|---|
| 修正 1（値の正規化と名前の正規名化を分ける） | deliverable |
| 修正 2（生成コードへの経路を全部つなぐ） | deliverable（BLUEPRINT §15.4 の範囲内） |
| 修正 3（fit / tune の両入口で学習前に拒否） | deliverable（順序テストは補助） |
| 修正 4（効き目・到達・生成 fitter の一致テスト） | **成果物に直結する補助** —— #277 の DoD「実行で示す」と一致 |
| 修正 5（受け継ぐ要件と新方針の区別、文書） | deliverable（H-0100 への記録は補助の手続き） |
| round 1 のその他の明確化 | deliverable。isotonic の sigmoid 記述の訂正は有界な隣接の後始末 |
| **round 1 後の拡大**（Platt 既定の変更・自前 MLE・artifact 移行） | **拡大した成果物 + それに伴う補助**。レジストリの網羅テストや記録は有界な補助 |

## 量の妥当性（監視の判定）

- **round 1 の修正の組は妥当。** 実行時と生成コードという実際の消費者を追っており、
  「受理されるが効かない設定」という失敗の形に正確に向けた証拠を用意している。汎用の検査機構を作る方向への転換は無い。
- **round 1 後の拡大は、この PR に対して妥当だと十分に正当化されていない。**
  研究記録（§4.2, §6）は既定の逸脱を**本 PR 以前からのもの**と書き、実測の差は小さく、既存利用者の結果が変わると認めている。
  fitter の置き換えは移行と数値検証の義務を新たに生む。**計画 Revision 6 §12.7 は PR 3c を #277 の「配線するか拒否するか」に限っている。**
  管理者の決定は意図を立証するが、それだけでは束ねることの妥当性を立証しない。

## Recommendation: `redirect`

> round 1 の修正は「パラメーターが効くこと」と「早期の拒否」に集中させたまま、
> fitter と既定値の置き換えを、互換性と検証の負担に見合う利益を示すべき**別のスコープ判断**として扱う。

## 主コンテキストの処分: **`continue`（redirect は採用しない）**

**理由**: 置き換えを本 PR に含めることは、管理者が**「別 PR で原典に寄せる」という選択肢を提示されたうえで、
既存利用者の結果が変わることを承知して**選んだ判断である（2026-09-15）。監視の指摘は「意図は立証されるが
妥当性は立証されない」であり、正しい区別だが、スコープの判断そのものは管理者のものである。

**監視の懸念への手当て（採用する）**:

1. **分割点を事前宣言する** —— 「platt の既定値変更・自前 MLE・旧 artifact 移行」は、
   「3 手法への params の反映・前処理の分離・入口検査・生成コードの再現」から**分けてコミットできる塊**として扱う。
   ラウンド予算 8 に達したとき、またはこの塊だけに検証の問題が残ったときは、**PR 3c-2 に切り出す**。
2. **計画書を実態に合わせる** —— `phase3-plan.md` §3 の 3c 行と §12.7 の「wire or refuse」の記述を、
   管理者の決定（反映する・既定を原典へ）に更新する。
3. 監視の「既定の逸脱による実測差は小さい」という点は、**H-0100 に数値ごと記録**し、変更の利益を過大に書かない。

**監視は verdict ではない。** 本節は主コンテキストによる reconcile である。
