# PR 2 — ループ監視（rounds 28-29、2026-09-09）

`policy:loop-monitor` に従い、**限定検証（round 30）を開く前に** relational monitor を
回した。read-only、fresh context（Codex `gpt-6-astra` effort low）。観測対象: rounds
**28-29**。capsule は `scratchpad/codex-monitor-prompt-round2930.md`。head `9267c9a`。

capsule には**主コンテキスト側から 1 つの事実を自己申告**した ——
**受け入れレビューが非 blocking と分類した 2 件（#288 = B4 / #287 = B3）を、
管理者の指示で本 PR 内で修正したこと**。そして「完了基準は機能した」「完了基準は機能して
いない（都合の悪い答えを出した瞬間に事前宣言が退けられるなら完了機構ではない）」の
**2 つの読みを両方提示し、記録がどちらを支持するかを判定させた**。

**1 回目は `INCONCLUSIVE` だった** —— capsule が記録のパスを
`docs/audits/2026-09-defect-discovery/` 接頭辞なしで `results/...` と書いていたため、
監視は 5 つの成果物を「見つからない」と正しく報告して `escalate` を勧告した。
**こちらの記述の誤りであり、監視は指示どおりに動いた。** パスを直して再実行した。

## VERDICT: **CONVERGING** ／ Recommendation: **`continue`**

> 選ばれた修正コミットに限定した検証を実施し、そのうえで管理者の受け入れ判断へ戻ること。
> **さらなる discovery ラウンドを自動的に開かないこと。**

## ラウンドごと

| 観測 | 内容 |
|---|---|
| **R28** | 環境依存の回帰テストを発見。**範囲を限定した production の検査は成立していた。** テストは修復され、停止条件 C が発火し、D14 の「別途承認された受け入れレビュー」に至った |
| **R29** | 最後の修復を確認し、**過大に述べられていた証拠のポインタを訂正**し、カバレッジの穴と探索空間の合成の失敗を特定。記録された処分は **B4 と非 blocking な B3 の分類を保持していた** |

## 収束

> **成果物の production の振る舞いは、この 2 ラウンドで前進していない** ——
> 転送・契約された正規化 surface・重複拒否はいずれも安定したままである。
> **証拠のほうは実質的に改善した** —— 対応づけの訂正と、#288 が入れた
> 「これまで未カバーだったマージ継ぎ目」のテストによって。
> **これはテストの periphery と分類されてはいるが、H-0094 に直接紐づく完了作業である。**

periphery も広がった: 文書・計測器・測定・categorical choice 検証への production 変更。
**後者は一部の拒否を前倒しするが、探索空間の正規化を 4 surface と整合させるものではない**
——H-0095 はその整合の問いを #287 として明示的に開いたままにしている。

**`0/54` の測定は観測されたスイートについてのものであり、ありうる全呼び出し元についてでは
ない**（監視が付けた bound。そのまま記録する）。

すでに clean と呼ばれた転送コンポーネントはより大きな PR の中に保持されたままである。
**それでも観測された拡大は有界である** —— clean と確認された head `9267c9a` で
`6b14b99` からの production 差分は **1 ファイル**、#284-#286 は除外されたまま。
**無制限な欠陥探索への回帰は観測されない。**

> **ここでの収束は「処分へ向かって狭まっている」ことを意味し、
> 「受け入れが完了した」ことを意味しない。**

## 自己申告した事実についての判定

> 記録は**第 1 の読みを支持する。ただし限定つき** ——
> **完了基準は「分類」の機構としては機能したが、「完了」の機構としての有効性は未証明である。**
> 指摘を blocking へ張り替えて追加作業を正当化した形跡は無い。
> 管理者のスコープ判断は実装より先に行われ、残る不整合は明示的に除外された。
> **承認された有限の追加は、それ自体ではループの再開を立証しない。**

### 監視がこちらの記述を訂正した点（重要）

> **しかし凍結は実際に破れており、round 29 が新しい head を証明することはできない。**
> さらに、**完了基準 §6 は「非 blocking な B3 は起票して終わり」「B4 は再レビューしない」
> と規定している。文書は依然として古い凍結 head と #287 のスコープ外処分を載せたままである。**
> **したがって今回の追随は、§6 の完了経路をそのまま実行したものではなく、その例外である。**

**こちらは「§6 の B1 が定める手順に入る」と述べていたが、それは誤りである。**
§6 は B3 を PR 内で直すことを規定していない。いま行っているのは
**管理者が承認した §6 への例外**であって、§6 の経路の実行ではない。
完了基準文書にそう書く（凍結 head の更新と #287/#288 の処分の書き換えと合わせて）。

### 第 2 の読みが支持される条件（監視の指定、そのまま）

> - 非 blocking な指摘が**繰り返し**必須の前提条件になる場合
> - **round 30 が選ばれた変更を超えて広がる**、またはさらなるラウンドを自動的に生む場合
> - 認識済みの周辺問題が開いたままでは明示的な受け入れ判断が下せない場合
>
> **「管理者が承認した」は権限を立証する。有界な follow-through だけが収束を立証する。**

## 主コンテキストの処分

1. **`continue` を採る** —— round 30 を**選ばれた修正コミットに限定**して開く
   （`77e02a8` と `9267c9a`、および #288 の `22b11b3`）。**それを超えて広げない。**
2. **完了基準を訂正する** —— §1 の凍結 head を更新し、§3 の #287/#288 を「本 PR で解決」に
   書き換え、**この追随が §6 の例外であることを明記する**（§6 をなぞったことにしない）。
3. **D14 に追記する** —— 非 blocking と分類した 2 件を管理者の指示で本 PR 内で修正した、
   という記録。
4. **`0/54` の bound を記録する** —— 観測されたスイートについての測定であり、
   ありうる全呼び出し元についてではない。
5. **round 30 のあと、さらなる discovery ラウンドは自動的に開かない。**

**監視は verdict ではない。** 本節は主コンテキストによる reconcile である。
