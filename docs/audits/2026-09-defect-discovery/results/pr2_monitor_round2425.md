# PR 2 — rounds 24-25 relational monitor（2026-09-08）

`policy:loop-monitor`。read-only、fresh context。観測範囲 **24-25**（25 側は
「round 24 のあと、round 25 を開くまでに行った契約の書き直し」）。head `85a7af0`。

## VERDICT: CONVERGING（構造的に。新しい契約に対してはまだ 1 ラウンドも走っていない）
## recommendation: **continue**

## ラウンドごと

- **R24**（`5a96daa`、非限定、依頼を契約検証として書き直したら Codex が完走）:
  `REQUEST_CHANGES` 2 件、どちらも `deliverable-path`、どちらも再現済み
  —— `PurePosixPath` と `10**5000` が入口を通り LightGBM の内側で raise した。
  修正は production `+43/−6`。**authorship 再帰 0/2 を監視が独立に確認**:
  `-S PurePosixPath` は `48a0801`、`_comma_form_matches` は `835398e` に当たり、
  どちらも H-0095 の当初実装であって round 23 の修正が書いたものではない。
- **R24 と現在のあいだ**（`5d3ee65..85a7af0`）: `git diff --stat -- lizyml/` は**空**。
  追加されたのは H-0095 §契約の確定（HISTORY +152）、BLUEPRINT §14.4（+3）、
  `instruments/param_domain_contract.py`（受理集合ブロックの生成 + `--check`、
  乖離で落ちることを実測）、母集団で量化したテスト 1 件
  （`test_every_accepted_value_can_be_written_as_json`、RED を 3 件 / 186 件で確認）、
  codex-home スクリプト 2 本（79 行）、D10（+57）。

## 収束の判定

**production コードを 1 行も変えない書き直しは apparatus ではなく成果物側の作業である**、
と監視は判定した。根拠 3 つ（いずれも実行して確かめたもの）:

1. **DC1 の「文法を閉じる」修復を、受入基準そのものに適用したもの**である ——
   ゲートの問いが「あらゆる Python オブジェクトを渡る反証不能な全称命題」から
   「受理母集団の上の有限のオラクル」に移った。
2. 新しいテストは**実在する消費者の要件**（`export_code` の `json.dump`。学習は
   成功したあとに落ちる形で観測された）を、報告された 1 型ではなく**母集団全体**で
   量化している —— **DC7 の durable repair の形そのもの**。
3. `56ff118` が示すとおり、instrument は**新しい事実を生んだ**（手書きの注が
   落としていた、位置で非対称な `longdouble` の件）。儀式ではない。

生成ブロック + `--check` は、2 度古びた散文を退役させている（DC3）。
この窓で純粋な apparatus は codex-home スクリプト 79 行だけで、それも R24 で
レビュアー側のセットアップ失敗が 13 回起きたことに由来する。

**周辺は成果物軸では横ばい** —— 直近 3 ラウンドの production 変更は 2 ファイルのみ、
どちらも deliverable path。新しいゲート機構も新モジュールも無い。

## 主文脈が保持すべき 2 つの事実

- **前回監視の反証条件 (b) は発火した**: `73a87b6` は `export_code` という
  2 つの `lgb.train` の外側の消費者のために受理集合を作り替えた。主文脈はこれを
  採用し、契約の書き直しがその処置である（§2 が `export_code` を母集団オラクル付きで
  名指した）。**(b) は生かしておくこと: R25 でどの消費者のためであれ受理集合が
  再び変わったら、それは DRIFTING である。**
- **#264 本体の修正は約 14 ラウンド clean でゲートも通っている。** base から 28 コミットが
  領域閉包のファイルを触っているが、`model.py` の最終変更は `48a0801`（H-0095 の入口）。
  D10 の選択肢 4（PR 分割）は管理者が検討のうえ却下したので、これは推奨ではなく記録である。

## R25 に向けた識別子（次の監視が使うもの）

- 「**§1/§3 の外側の値 X が検査を破る**」形の指摘 → **書き直しが効いていない** → DRIFTING。
- 「**§2 の要件 N が受理母集団の上で成り立っていない**」または「**§2/§3 が不完全**」
  → 契約の内側 → `REQUEST_CHANGES` であっても CONVERGING。

## 主文脈の処置

**continue を採用する。** そのうえで監視の指示どおり、**依頼に「契約そのものを
拒否してよい」と明記する** —— 固定するテストの無い受理集合の行、§2 に欠けている消費者、
DC7 的に達成不能な §3 の bound、生成ブロックと矛盾する §1 の行。
**これを書かないと、失敗した成果物の作者が受入基準を書き換えた形**になり、
D10 が利益相反として却下した選択肢 D と区別が付かなくなる。
