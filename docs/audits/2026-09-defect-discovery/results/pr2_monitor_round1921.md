# PR 2 — rounds 19-21 relational monitor（2026-09-08）

`policy:loop-monitor`。read-only、fresh context、`templates/review-loop-monitor-capsule.md`
に沿った capsule。観測範囲 **19-21**。head `8f63a02`。

**この監視には反対仮説を明示的に渡した**（PR 2 で有効だったやり方）:

> 修正が失敗し続けた maker が、apparatus を膨らませることで応じたのではないか —
> 新モジュール 1 本、テスト約 2100 ケース、出口の表明、書き換えたオラクル。

加えて、自己レビューが実装後に 5 件見つけて直したことも渡した。

## VERDICT: CONVERGING / recommendation: continue

## 監視が出した根拠（要点）

**反対仮説は実測で覆された。** `1403ba8..HEAD` の全リポジトリ差分:
production 17511 → 17604（**+93**）、tests 36309 → **36105（−204）**。
`value_equality.py` 494 → 140、`param_domain.py` 373 が新規。
「約 2100 の新ケース」は `ACCEPTED_POPULATION` の**生成された直積**であって手書きの
apparatus ではない — **ケース数は 3 倍になりながらテストの行数は減った**。

**成果物の上にある。** H-0095 が触るのは `model.py` / `_model_factories.py` /
`adapter.py` / `isotonic.py` と新モジュールだけ ＝ 成果物が名指す 4 surface と
2 つの学習サイトそのもの。

**閉包は効いている場所で本物。** 受理は**厳密な型一致**（`type(v) in ...`、
`isinstance` を使わない: `param_domain.py:125,145,181,213,252`）。これは
rounds 16-20 が追い続けた軸 —— `__format__` / `__str__` / `__eq__` を上書きする
サブクラス —— を**構成上消している**。

**自己レビューの 3 件を、丸めずに分けた:**

1. `set` の向きが逆で記録がコードと矛盾していた → **DC3、記録層**。出荷される振る舞いには影響なし。
2. mapping の拡大 → **出荷済みの形式**（H-0065 `metric={...}`）を拒否するところだった。
   閉包を実入力に当てたから出荷前に見つかった。**閉包に有利な証拠**。
3. 深さ 3 → **ループ自身のクラス**。母集団がその形を生成していなかった。
   反対仮説に有利な唯一の点。

**ただし残存リスクの向きが変わった。** 母集団の**原子**は型表から導出されているが、
**形**は 8 項目の宣言リスト（`test_param_domain.py` の `_sequences_of`。今は深さ 3 /
`set` / mapping を含む）。列挙されていない 9 つ目の形はありうる。**しかしモジュールは
未知の構造に対して fail closed なので、次の 1 件は「過剰拒否（DC7）」であって
「`lgb.train` に届く静かな wire 変化（DC1）」ではない。rounds 16-20 は全て DC1 で、
全て学習済みモデルに到達していた。** 失敗クラスが変わったこと自体が、
「同じサイクルの引っ越し」ではなく「閉包」である理由。

**記録のみ（採点ではない）**: skip が 62 → 256。増分の大半は mapping を含む候補が
wire 保存の主張を skip するもの（`test_param_domain.py`）。mapping は自身として
シリアライズされないので正しいが、**それらのケースは wire 保存について何も証拠を出さない**。

## 監視が事前に宣言した反証条件（round 21 の最重要問い）

> **CONVERGING は、round 21 が「導出が生成しなかった *形* であって、それを受理すると
> `lgb.train` に届く bytes が変わるもの」を見つけたときに反証される。**
> 記録・ドキュメント層の指摘や、正当な形の過剰拒否は**反証しない** — それらは新しい、
> より安いクラスである。18-19 監視が言い、今も正しいこと: **blocking 件数の増減は
> 何も区別しない。**

## main context の処置（`policy:main-context-ownership`）

**全面採用。** 反証条件を round 21 prompt の「事前に宣言する最重要の問い」として
そのまま載せ、母集団の**形が宣言リストである**ことも敵対的入力として開示した。
監視が名指しで求めた **findings の層タグ**（`deliverable-path` / `periphery`）も
prompt に必須として入れた。skip の増加も開示項目に加えた。

監視が verdict を決めたのではなく、こちらが独立に判断して採用している。
