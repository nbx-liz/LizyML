# 次の一手 — 2026-09-08（作業中断時点の単独ハンドオフ）

このファイルだけ読めば次の作業に入れるように書いてある。

---

## ⛔ 最初にやること — **D13 が開いたまま、管理者の判断待ち**

**判断が来るまでコードを触らないこと。** 何かを直しに行くと、判断の対象そのものが動く。

### 問われていること

**「門を足したが 1 か所を通していない」という同じクラスが 2 連続で出た**あと、
どう進めるか。選択肢と実測は `DECISIONS-PENDING.md` の **D13**。

1. **位置を計測にしてから round 27** ← **監視と外部評価の両方が推す**
2. そのまま round 27 を範囲限定で回す（D12 の決定を維持）
3. PR を分割する（#264 本体を先にマージし、H-0095 を別 PR へ）
4. `APPROVE` を求めずマージする

### 選択肢 1 を選ぶ場合、**満たすべき条件が決まっている**（外部評価が指定した）

`results/pr2_situation_assessment.md` に全文。要点だけ:

- **どの要件がどの位置に適用されるか**を確立し、**強制を実際に行使し**、
  **門が迂回されたら落ちる**こと。**ソース中にヘルパ呼び出しを見つけるだけでは不十分。**
- 3 つの構造走査（`normalise_value` / `_holds_a_mapping` / `_is_unchanged`）は
  **仕事が違う**。同じ答えを返すことではなく、**意図された関係**を確かめること。
- テスト母集団は **numpy 型軸は導出しているが、コンテナ形状は手で作っている**。
  この差が欠陥クラスに直接効いている。
- **計測自身が別の不完全な列挙になり自分を証明する**のが最大のリスク。
  範囲を区切り、迂回に対する感度を確かめ、独立した振る舞いオラクルを残すこと。
- **選択肢 1 は唯一の形ではない** —— **共有の強制点を作って迂回を構造的に不可能にする**
  形もある（そちらのほうが強い）。

---

## 状態（2026-09-08 中断時点、すべて実測）

| 項目 | 状態 |
|---|---|
| ブランチ | `fix/phase3-pr2-fit-params-forwarding` |
| head | **`8d3d831`**、`origin` と一致、**作業ツリー clean** |
| PR **#278** | **draft** |
| CI | **13/13 全緑**（`7ee10fe`。macOS / Windows smoke 含む。以後は docs のみ） |
| フルスイート | **9731 passed / 278 skipped** |
| ruff / ruff-format / mypy | clean |
| `param_domain_contract.py --check` | exit 0 |
| `report_lifecycle_grid.py` | exit 0（最後に確認したのは契約書き直し前） |
| **Codex `APPROVE`** | **未取得（26 ラウンド）** |

**走っているバックグラウンド処理は無い。** 監視も CI watch も完了済み。

---

## このセッションでやったこと（時系列、すべてコミット済み）

1. **D10 = 選択肢 2**（止めて契約を書き直す）。契約の書き直しを実施:
   - `HISTORY.md` H-0095 に **「契約の確定」節**（受理集合を位置 × 厳密型で列挙し
     各行を固定するテストを名指す / 消費者を全列挙し要件ごとに母集団オラクル /
     スコープ外の明示）。旧受理表には superseded の印。
   - **受理集合ブロックは生成物** —— `instruments/param_domain_contract.py` が
     `param_domain.py` から生成、`--check` が乖離で非零終了（DC3）。**落ちることを実測。**
   - **`json.dump` 要件のオラクル**を追加（RED を 2 通りで確認）。
   - `BLUEPRINT.md` §14.4 に同じ 3 点を追記。
   - 副産物: sink 走査の初版が calibrator の `lgbm.train` を別名ゆえに落としていた /
     手書きの「型集合 > 値集合」注が `longdouble`（位置で非対称）を落としていた。
     どちらも導出・計測に置換。
2. **B / C / D を回答**（D10 の続き）: **B 認めない**（マージゲートは Codex `APPROVE` のみ）/
   **C authorship 停止条件**（round N の修正が書いたコードの欠陥が N+1 で出たら停止）/
   **D 現方針維持**（#283 は H-0095 では解決しない）。
3. **round 25**（非限定、契約に対する最初のラウンド）: `REQUEST_CHANGES` 3 件。
   **3 件とも契約の内側の形**、「§1/§3 の外側の値が破る」形は 0 件。
   → **C が発火**（指摘 2 が round 24 の修正由来）→ **D11**。
4. **D11 = 3 件直して範囲限定 round 26**。修正: UTF-8 要件 / 述語を正規化関数経由に /
   母集団の型軸を導出。フルスイート 9710。
5. **D11 の続き**: 監視が**監視自身の欠陥**を指摘 —— 反証条件を 3 監視が 3 通りに
   書いていた。**1 つの文言に固定**（「ラウンド開始時点の §2 の表に無い sink を
   名指したら DRIFTING」）。
6. **round 26**（範囲限定）: `REQUEST_CHANGES` 2 件。**指摘 2 は `65dfdbe` が書いたコード**
   → **C と監視の take-stop が両方発火** → **D12**。
7. **D12 = 2 件直して範囲限定 round 27**。修正: numpy 要素を同じ門に通す /
   `repr` 判定を**型の再帰的一致 + `is`** に置換。フルスイート 9731、CI 13/13。
8. **round 27 前の監視が D12 の前提を覆した** → **D13**（下記）。
9. **ユーザー指示で、状況そのものを Codex Astra low に評価させた**
   → `results/pr2_situation_assessment.md`。**選択肢 1 を推奨、ただし主張は狭めること。**

---

## 外部評価がこちらの記述を訂正した 4 点（繰り返さないこと）

1. **「位置の計測」はクラス単位の修復の唯一の形ではない。** 共有の強制点で迂回を
   構造的に不可能にする形がある。**ソース由来のテストは証拠であって閉包ではない。**
2. **領域が無限でも、理由をもった承認は不可能ではない。** 網羅テストは全称命題を
   確立できないが、**実装の議論は支持しうる**。**問い方を変えただけでは解けていない。**
3. **rounds 1-20 は一様な「敵対的な値」の演習ではなかった**（D7 は転送・永続化・export の
   作業を記録）。**verdict を伴う完走が 26 回あったわけでもない**（中断ラウンドがある）。
4. **「round 15 以降 clean」は分割を支持するが、切り出した PR が承認済みという意味ではない。**

---

## 記録の所在

- ラウンド: `results/pr2_codex_round[1-26].md`
- ループ監査: `results/pr2_monitor_round*.md`
  **⚠️ `pr2_monitor_round23.md` は rounds 2-3 の監査。** 直近は
  **`pr2_monitor_round2527.md`**（その前が `2426`、さらに前が `2325`）
- 状況評価: **`results/pr2_situation_assessment.md`**
- 判断: `DECISIONS-PENDING.md` の **D7 / D8 / D9 / D10 / D11 / D12 / D13**（D13 が開いている）
- 提案: `HISTORY.md` の **H-0094**（決定 1-17）と **H-0095**
  （提案 → 補正 1-19 → **契約の確定**。**正は「契約の確定」節**）
- レビュー依頼の雛形: `prompt-templates/pr2-review-round24-completed.md`（初めて完走した版）、
  `pr2-review-round25.md`（契約検証の版）、`pr2-review-round26.md`（範囲限定の版）

---

## 積み残し（PR 2 由来、起票済み）

**#281**（loaded model の `validation_ratio`）/ **#282**（`category: training` の `seed` 次元）/
**#283**（スカラー vs 単一要素の列、D10 で「H-0095 では解決しない」と決定）/
**#277 / #279 / #280**

## この run のあと

PR 3（#258 tuning direction）、PR 3b（H-0024 space merge、`HISTORY.md:1615` と `:1616` の
矛盾を解消すること）、PR 4-9。**PR 9 の直前に繰り延べ 1 件**: Phase 3 完了測定ツール
（`phase3_gap.py` + manifest）は `instruments/deferred/` に未出荷で archive。

---

## 環境メモ（踏むと時間を失う）

- `uv` は読み取り専用の既定キャッシュで落ちる → **`UV_CACHE_DIR="$TMPDIR/uv-cache"`**。
  pre-commit hook も同じで、**git-manager にこの指示を毎回渡すこと**
- **コマンドガードが引用符・アポストロフィを解析できずに拒否する** →
  **ヒアドキュメントに引用符を入れない / スクリプトファイルにして実行 /
  コミットメッセージにアポストロフィを入れない**
- **Codex は `instruments/setup_codex_home.py` で `CODEX_HOME` の書き込み可能コピーを
  作ってから実行し、`cleanup_codex_home.py` で消すこと**（両方このセッションで出荷した。
  以前は scratchpad にあり 2 度失われている）。既定 effort は `low`
- codex の長時間実行は**フォアグラウンドで `timeout` を長めに**（バックグラウンドだと
  低メモリ判定で kill された）。レビュー 1 ラウンドは effort medium で 10 分以内に収まる
- **監視を回している間はファイルを編集しないこと**（capsule に「worktree clean」と
  書いたまま並行編集して指摘された）
- フルスイートは `instruments/run-exclusive.sh` 経由で起動する（約 2 分）
- `gh pr checks <N> --watch` を `run_in_background` で使うとき、
  **そのシェルに `$TMPDIR` は無い** —— 出力先は絶対パスで書くこと

---

## この PR が実際に含んでいるもの

- **#264 / H-0094**（rounds 1-15 で決着）: `fit(params=)` が学習に届く。
  パラメーター層は**綴りではなく同一性**でマージ（LightGBM がエイリアスを解決するため）
- **H-0095**: `lizyml/core/param_domain.py`。4 surface の入口で 1 度だけ正規化し、
  受理集合の外は学習前に `CONFIG_INVALID`。`lgb.train` の 2 か所に出口の表明を置き、
  **学習サイトの母集団をソースから導出**して固定（配線の主張を**性質**へ）。
  `value_equality.py` は 494 行 → 約 140 行

**中心的な不変条件は wire 保存**: `_param_dict_to_str` が正規化の前後で同じ bytes。
シリアライザは**位置ごとに別のフォーマッタ**を使う（スカラー `__format__` / 要素 `str`）。

**意図的な narrowing（すべて実測付き）**: `set`（列は位置依存 / set に順序が無い）、
リストの入れ子は深さ 2 まで、`np.timedelta64` / `np.datetime64`、numpy スカラーと
`ndarray` の**サブクラス**、pure path、文字を作れない値。
**`Path` は受理してテキストに変換**（`export_code` の `json.dump` が運べる唯一の形）。

---

## Codex 運用メモ（rounds 22-24。踏むと時間を失う）

**Codex はレビュー依頼の書き方で provider 側のコンテンツフィルタに落ちる。**

```
ERROR: This content was flagged for possible cybersecurity risk.
```

rounds 22 / 23 / 23b が中断。**中立的な質問で同じファイルを読ませたら完走した**ので、
反応しているのは**コードではなく prompt**。落ちた書き方に共通していたもの:

- 過去のすり抜けを並べた表（「何が通り / 何と書かれ / 何で学習したか」）
- 「呼び出し元のコードが走る経路はあるか」という問い
- 「2 回外している。3 回目を狙ってほしい」という煽り

**通る書き方**: 契約を述べて「守られているか」を問う。受け入れ基準の列挙も
narrowing の一覧も、測定値付きの事実として書けば通る。
**完走した prompt をリポジトリに置いた**:
`prompt-templates/pr2-review-round24-completed.md`。**次はこれを雛形にすること。**

**中断しても必ずログを読むこと** — round 22 は verdict を返さなかったが、中断前に
再現し終えた本物の欠陥が 1 件ログに残っていた。

**代替経路**: `policy:fresh-checker` の `agent:general-purpose` を read-only、同一
capsule で走らせられる（round 23 はこれ）。**ただし Codex ではないのでゲートを
満たさない**（→ 判断 B）。

---
