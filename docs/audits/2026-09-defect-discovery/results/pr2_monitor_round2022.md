# PR 2 — rounds 20-22 relational monitor（2026-09-08）

`policy:loop-monitor`。read-only、fresh context。観測範囲 **20-22**。head `40783e7`。

**この監視には「前回の監視が事前に宣言した反証条件が round 21 で発火した」事実を
そのまま渡した。** 自分の解釈（「比較の欠陥ではなく入口の型判定の欠陥であり、
修正は構成上のもの」）も渡したうえで、**採用しないよう明示した。**

## VERDICT: CONVERGING / recommendation: continue

## 監視が出した根拠（要点）

**反証条件が発火したことを和らげなかった。** 前回の監視は 5 か所の行番号で厳密型一致を
確認して「閉包は本物」と述べたが、**6 か所目が `isinstance` で、DC1 はまさにそこから
逃げた**。つまり誤っていたのは設計ではなく**部分的な検証の上に立てた主張**である。
その区別が verdict の全体であり、**同じ誤りを繰り返さない義務**として:

> **`np.ndarray` の `isinstance` 受理が 2 か所残っている（`param_domain.py:270,287`）。
> この監視はそれが安全だと検証していない。**

**D7 の authorship 条件は文面上 round 21 の 3 件すべてに当たる**（`param_domain.py` は
新規、`value_equality.py` は書き換え）。しかし条件の趣旨は**修正の再帰**であり、
**着地したばかりの設計変更に対する最初の unscoped レビューは修正ラウンドではない**。
**`param_domain.py` の authorship カウンタは 0。round 22 がその最初の修正ラウンドになる。**

**成果物は動いた**: numpy の門が「もう 1 つの guard」ではなく**導出**で閉じ、宣言した
全域性が真になり、導出テストが狭まりだけでなく**広がり**も検出するようになった。
**周辺は横ばい**: tests 36158 → 36105 → 36226、production は round 21 で +72 行、
監査ドキュメント +163、新規モジュールなし、新規計測器なし。

## 監視が事前に宣言した反証条件（round 22 以降）

どちらかが発火したら `take-stop-condition`:

- **(a) authorship** — `1147c98` または `051b2fa` が書いたコードに blocking が出たら。
  新モジュールでの修正の再帰であり、rounds 16-20 の再現である。
- **(b) DC1** — 4 surface のいずれかで受理された値が、**未検証の 2 つの
  `np.ndarray` `isinstance` 門を含めて**、`lgb.train` に届く bytes を呼び出し元の
  値が書くものと違えたら。

**反証しない**: 正当な形の過剰拒否（DC7）、記録・ドキュメント層の指摘、成果物の外の
既存コードの指摘。**blocking 件数だけでは何も区別しない。**

## main context の処置（`policy:main-context-ownership`）

**`continue` を採用し、監視が名指しした未検証の門を round 22 の前に自分で検証した。**

**1 件見つかった（監視の prong (b) そのもの、修正済み）。** 1-D の `ndarray`
**サブクラス**で `__iter__` が呼ぶたび別の列を返すものは受理され、**wire が変わる**:

```
値                                    caller が書く wire   正規化後の wire
1-D ndarray サブクラス（__iter__ が毎回変わる）   1.0,1.0          2.0,2.0
```

`np.matrix` / masked array / `ndim` が嘘をつくサブクラスは元から拒否されていた。
修正はスカラーの門と同じ形 — **`type(value) is np.ndarray`（厳密型一致）**、
および 1-D 判定をシリアライザ自身の述語に合わせて `len(shape)` で行う
（`ndim` と `shape` は numpy の配列では一致するが、他では一致する保証がない）。
RED 検証で赤を確認。

**この 1 件は round 22 の反証条件 (a) には当たらない** — `isinstance` の門は
`1147c98` / `051b2fa` が書いたものではなく、H-0095 の最初の実装から在ったものである。
