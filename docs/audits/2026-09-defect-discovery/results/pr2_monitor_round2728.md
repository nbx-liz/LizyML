# PR 2 — ループ監視（rounds 26-27、2026-09-09）

`policy:loop-monitor` に従い、round 28 を開く前に relational monitor を回した。
read-only、fresh context。観測対象: rounds **26-27** と、その間に入った仕様交代。
capsule には**主コンテキスト側から 2 つの事実を自己申告**した ——
(a) round 27 は規則が入れ替わった後の最初のラウンドであること、
(b) 報告された 1 か所に加えて**もう 1 か所を自分で見つけて一緒に直した**こと。
後者が「クラス単位の修復」なのか「範囲の拡大」なのかを判定させるためである。

## VERDICT: **CONVERGING** ／ Recommendation: **`continue`**（条件つき）

## ラウンドごと

| 観測 | 内容 |
|---|---|
| **R26**（`e52f062`、`65dfdbe` に限定） | `REQUEST_CHANGES` ×2、どちらも `param_domain.py` 内・`deliverable-path`。**停止条件 C と監視の take-stop が両方発火**し、round 27 は開かれなかった |
| **仕様交代**（`3fba763`→`559fa8e`→`9d33737`） | H-0096 受理。`value_equality.py` **−157 行**＋テスト −457 行、`BLUEPRINT.md` §14.4 改訂、§2 消費者行 7 を superseded。**D13 の `### 決定` が書かれ、前回監視が `escalate` した記録の穴は閉じた** |
| **R27**（`5e23617`、H-0096 に対する最初のラウンド） | `REQUEST_CHANGES` ×1、P2。拒否**メッセージ**が `str(値)` に依存していた。修復は `5715ee2` |

## 収束

**deliverable は縮んだ。** R27 の修復は production **33 行**
（`_model_factories.py` +17 / `adapter.py` +16）とテスト 60 行のみで、
**新規モジュール・新規 instrument・新規分析文書はゼロ**。
`param_domain.py` は**未変更**（H-0096 受け入れ基準 6 が保たれていることを diff で確認）。

**修復の形が前と逆向きである、という判定**: 「各位置に 3 つ目の門を足す」のではなく
**message と context の両方から値そのものを外した**。R25→R26 で 2 連続した
「門を足して 1 か所を漏らす」形とは反対向きであり、3 か所目
（`LizyMLError.__repr__` の `!r`）は**入力を絶ったので開かない**。
両呼び出し点は H-0096 のスコープ行が名指した 2 ファイルなので、
**範囲の拡大ではなくクラス単位の修復**である。

**D11 の反証条件**: 字義どおりなら「拒否メッセージ」はラウンド開始時点の §2 の表に無い。
ただし §2 は**受理値の消費者**の表であり、R27 は受理集合を 1 件も変えていない
（要件の追加ではなく**読みの除去**）。よって**発火しない**と判定された。

## authorship 条件 C についての、監視からの警告

**C も発火していない。** round 27 は直前の修正ではなく、
**principal が根本原因解析と先行事例調査のあとに選んだ新しい規則**に対する
最初のラウンドだからである（D13 の決定が現存する）。

> **リセットはここで使い切っている。** round 28 が `5715ee2` の書いたコードの
> 欠陥を出せば C は再度発火し、**次はリセットの根拠が無い。**

## 監視が出した条件（round 28 の前に閉じること）

`HISTORY.md` の H-0096 提案が **「拒否メッセージは surface と綴りと値を名指す」**
と書いたままで、出荷実装と食い違っていた。SSOT ↔ 実装の 1 行の drift（**DC3 の形**）。
verdict は変えないが、round 28 を開く前に訂正すること。

## 主コンテキストの処分

**条件を満たした** —— `HISTORY.md` の当該行を、round 27 が差し戻した事実ごと訂正した
（「値は名指さない。context からも外す。`__repr__` が `!r` で描画するため」）。

そのうえで **round 28 を `5715ee2` に範囲限定で開く**。
C についての警告は round 28 の判断材料としてそのまま持ち越す。

**監視は verdict ではない。** 本節は主コンテキストによる reconcile である。
